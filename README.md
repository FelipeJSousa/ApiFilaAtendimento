# Api Fila Atendimento
Projeto desenvolvido para estudo de APIs em Python utilizando FastApi.

# Run 

`python -m venv .venv`

`.venv/Scripts/Activate`

`python pip install requirements.txt`

`python pip install dev-requirements.txt`

`uvicorn main:app`
